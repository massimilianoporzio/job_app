import 'package:flutter/material.dart';

class CardFreelancer extends StatelessWidget {
  const CardFreelancer({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder(
      color: Colors.amber,
    );
  }
}
