import 'package:flutter/material.dart';

class AnnunciFreelancers extends StatelessWidget {
  const AnnunciFreelancers({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Icon(
        Icons.account_circle,
        size: 40,
      ),
    );
  }
}
