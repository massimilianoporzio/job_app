import 'package:flutter/material.dart';

class AnnunciPreferiti extends StatelessWidget {
  const AnnunciPreferiti({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Icon(
        Icons.bookmark,
        size: 40,
      ),
    );
  }
}
